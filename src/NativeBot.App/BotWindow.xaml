<Window x:Class="NativeBot.App.BotWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Бот" Height="720" Width="1120" MinHeight="560" MinWidth="900" WindowStartupLocation="CenterOwner">
  <DockPanel Margin="14">
    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
      <TextBlock x:Name="BotTitle" FontSize="24" FontWeight="SemiBold" VerticalAlignment="Center" />
      <Button Content="Запустить" Click="Start_Click" />
      <Button Content="Остановить" Click="Stop_Click" />
      <TextBlock x:Name="RuntimeStatus" Margin="12,0" VerticalAlignment="Center" Foreground="DimGray" />
    </StackPanel>
    <TabControl>
      <TabItem Header="Конструктор">
        <Grid Margin="8">
          <Grid.ColumnDefinitions><ColumnDefinition Width="230"/><ColumnDefinition Width="*"/><ColumnDefinition Width="300"/></Grid.ColumnDefinitions>
          <DockPanel Grid.Column="0" Margin="0,0,10,0">
            <TextBlock DockPanel.Dock="Top" Text="Блоки" FontSize="18" FontWeight="SemiBold" Margin="4" />
            <ListBox x:Name="PaletteList" DisplayMemberPath="Label" MouseDoubleClick="AddNode_Click" />
            <Button DockPanel.Dock="Bottom" Content="Добавить блок" Click="AddNode_Click" />
          </DockPanel>
          <DockPanel Grid.Column="1">
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
              <TextBox x:Name="FlowNameBox" Width="250" Text="Новый сценарий" Padding="6" />
              <Button Content="Сохранить черновик" Click="SaveDraft_Click" />
              <Button Content="Проверить" Click="Validate_Click" />
              <Button Content="Опубликовать" Click="Publish_Click" />
            </StackPanel>
            <ListBox x:Name="NodesList" DisplayMemberPath="Name" SelectionChanged="NodesList_SelectionChanged" Margin="0,8" />
          </DockPanel>
          <StackPanel Grid.Column="2" Margin="10,0,0,0">
            <TextBlock Text="Настройки блока" FontSize="18" FontWeight="SemiBold" />
            <TextBlock Text="Содержимое" Margin="0,12,0,3" />
            <TextBox x:Name="ContentBox" AcceptsReturn="True" Height="120" TextChanged="ContentBox_TextChanged" />
            <TextBlock Text="Введите {, чтобы увидеть переменные" Foreground="DimGray" Margin="0,4" />
            <ListBox x:Name="VariableHints" Height="90" Visibility="Collapsed" MouseDoubleClick="VariableHints_MouseDoubleClick" />
            <Expander Header="Дополнительно" Margin="0,12,0,0">
              <StackPanel Margin="8">
                <CheckBox Content="Отключить уведомление" />
                <CheckBox Content="Защитить содержимое" />
                <TextBox ToolTip="Тема сообщения" Margin="0,6" />
              </StackPanel>
            </Expander>
            <Button Content="Применить к блоку" Click="ApplyNode_Click" />
            <ListBox x:Name="ValidationList" Height="150" DisplayMemberPath="Message" />
          </StackPanel>
        </Grid>
      </TabItem>
      <TabItem Header="Пользователи">
        <DockPanel Margin="8">
        <StackPanel DockPanel.Dock="Top">
          <StackPanel Orientation="Horizontal"><TextBox x:Name="UserSearchBox" Width="260" Padding="6" ToolTip="ID, username или имя" /><Button Content="Найти" Click="RefreshUsers_Click" /><Button Content="Сбросить" Click="ResetUsers_Click" /></StackPanel>
          <Expander Header="Фильтры" Margin="0,6,0,6">
            <WrapPanel Margin="8">
              <TextBox x:Name="UserLanguageBox" Width="90" ToolTip="Язык, например ru" />
              <ComboBox x:Name="UserStatusBox" Width="150" ToolTip="Состояние"><ComboBoxItem Content="Любое состояние" IsSelected="True"/><ComboBoxItem Content="Активен"/><ComboBoxItem Content="Бот заблокирован"/><ComboBoxItem Content="Чат недоступен"/></ComboBox>
              <DatePicker x:Name="UserActiveAfterBox" Width="130" ToolTip="Активен после даты" />
              <ComboBox x:Name="UserTagBox" Width="150" DisplayMemberPath="Name" ToolTip="Тег" />
              <ComboBox x:Name="UserFieldBox" Width="150" DisplayMemberPath="Name" ToolTip="Пользовательское поле" />
              <TextBox x:Name="UserFieldValueBox" Width="130" ToolTip="Значение поля" />
              <ComboBox x:Name="UserSegmentBox" Width="160" DisplayMemberPath="Name" ToolTip="Сохранённый сегмент" />
            </WrapPanel>
          </Expander>
        </StackPanel>
        <DataGrid x:Name="UsersGrid" IsReadOnly="True" AutoGenerateColumns="False" MouseDoubleClick="UsersGrid_MouseDoubleClick">
          <DataGrid.Columns>
            <DataGridTextColumn Header="Telegram ID" Binding="{Binding TelegramId}" Width="*" />
            <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*" />
            <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="*" />
            <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="*" />
            <DataGridTextColumn Header="Язык" Binding="{Binding Language}" Width="*" />
            <DataGridTextColumn Header="Первое взаимодействие" Binding="{Binding FirstSeenAt}" Width="2*" />
            <DataGridTextColumn Header="Последнее взаимодействие" Binding="{Binding LastSeenAt}" Width="2*" />
            <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="*" />
          </DataGrid.Columns>
        </DataGrid>
        </DockPanel>
      </TabItem>
      <TabItem Header="Рассылки">
        <Grid Margin="12"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
          <StackPanel>
            <TextBox x:Name="BroadcastNameBox" Padding="6" ToolTip="Название рассылки" />
            <TextBox x:Name="BroadcastTextBox" Height="90" Margin="0,8" Padding="6" AcceptsReturn="True" ToolTip="Текст сообщения" TextChanged="BroadcastTextBox_TextChanged" />
            <StackPanel Orientation="Horizontal"><Button Content="Тест выбранному пользователю" Click="TestBroadcast_Click"/><Button Content="Запустить всем найденным" Click="StartBroadcast_Click"/></StackPanel>
          </StackPanel>
          <DataGrid x:Name="BroadcastsGrid" Grid.Row="1" Margin="0,10,0,0" IsReadOnly="True" AutoGenerateColumns="True" />
        </Grid>
      </TabItem>
      <TabItem Header="Журнал">
        <DockPanel Margin="12"><StackPanel DockPanel.Dock="Top" Orientation="Horizontal"><CheckBox x:Name="TechnicalLogBox" Content="Технические сведения" Click="RefreshLogs_Click"/><Button Content="Обновить" Click="RefreshLogs_Click"/></StackPanel><DataGrid x:Name="LogsGrid" IsReadOnly="True" AutoGenerateColumns="True" /></DockPanel>
      </TabItem>
      <TabItem Header="Настройки">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
          <TextBlock Text="Как получать события" FontWeight="SemiBold" />
          <ComboBox x:Name="UpdateModeBox" Width="260" HorizontalAlignment="Left" Margin="0,6,0,0">
            <ComboBoxItem Tag="Webhook" Content="Webhooks" />
            <ComboBoxItem Tag="LongPolling" Content="Long polling" />
          </ComboBox>
          <Grid Margin="0,12,0,8" MaxWidth="820" HorizontalAlignment="Left">
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
            <StackPanel Margin="0,0,14,0">
              <TextBlock Text="Выберите webhooks, если:" FontWeight="SemiBold" />
              <TextBlock Text="☐ Продакшн с высокой нагрузкой (&gt;500 событий/день)&#10;☐ Нужна минимальная латентность&#10;☐ Есть домен и SSL-сертификат&#10;☐ Масштабируемость критична&#10;☐ Платформа рекомендует webhooks" TextWrapping="Wrap" LineHeight="22" />
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="14,0,0,0">
              <TextBlock Text="Выберите long polling, если:" FontWeight="SemiBold" />
              <TextBlock Text="☐ Разработка/тестирование&#10;☐ Нет доступа к домену/SSL&#10;☐ Низкая нагрузка (&lt;500 событий/день)&#10;☐ Один бот без масштабирования&#10;☐ Internal-бот без доступа в интернет" TextWrapping="Wrap" LineHeight="22" />
            </StackPanel>
          </Grid>
          <CheckBox x:Name="BotAutoStartBox" Content="Запускать этого бота автоматически" Click="BotAutoStart_Click" />
          <CheckBox x:Name="PrivacyModeDisabledBox" Content="Privacy Mode отключён в BotFather (бот читает сообщения групп)" Click="PrivacyModeDisabled_Click" Margin="0,12,0,0" />
          <CheckBox x:Name="AgentAutoStartBox" Content="Запускать Bot Agent вместе с Windows" Click="AgentAutoStart_Click" Margin="0,12,0,0" />
          <TextBlock Margin="0,16,0,4" Text="Публичный HTTPS-адрес webhook" />
          <TextBox x:Name="WebhookPublicUrlBox" Width="500" HorizontalAlignment="Left" ToolTip="Например: https://bots.example.com" />
          <TextBlock Margin="0,12,0,4" Text="Локальный HTTPS-адрес Bot.Agent" />
          <TextBox x:Name="WebhookListenUrlBox" Width="500" HorizontalAlignment="Left" Text="http://localhost:8443" />
          <TextBlock Margin="0,5,0,0" Foreground="DimGray" Text="Туннель должен направлять публичный адрес на локальный HTTPS. Изменение локального адреса применяется после перезапуска Agent." />
          <Button Content="Сохранить способ получения" Width="230" HorizontalAlignment="Left" Click="SaveWebhookSettings_Click" />
          <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
            <Button Content="Экспортировать проект" Click="ExportProject_Click" />
            <Button Content="Импортировать проект" Click="ImportProject_Click" />
          </StackPanel>
          <TextBlock Margin="0,16,0,4" Text="Поведение при закрытии окна" />
          <ComboBox x:Name="CloseBehaviorBox" Width="320" HorizontalAlignment="Left" SelectionChanged="CloseBehavior_Changed">
            <ComboBoxItem Tag="ask" Content="Всегда спрашивать" IsSelected="True" />
            <ComboBoxItem Tag="background" Content="Продолжать работу в фоне" />
            <ComboBoxItem Tag="exit" Content="Полностью завершать приложение и ботов" />
          </ComboBox>
        </StackPanel>
        </ScrollViewer>
      </TabItem>
    </TabControl>
  </DockPanel>
</Window>
